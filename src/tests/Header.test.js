/*
import React from 'react';
import TestRenderer from 'react-test-renderer';
import Foods from '../pages/Foods';
import HeaderContext from '../context/HeaderContext';

describe('Requisito 9 - Implemente os elementos de header na tela principal', () => {
  const food = new TestRenderer.create(
    <HeaderContext.Provider value="comidas">
      <Foods />
    </HeaderContext.Provider>,
  );
  expect(food).toBeInTheDocument();
});

  it('Verificar se existem  na tela file-top-btn, page-title e search-top-btn', () => {
    const { getByTestId, history } = renderWithRouter(<App />);
    history.push('/comidas');
    const title = getByTestId('page-title');
    const profile = getByTestId('profile-top-btn');
    const search = getByTestId('search-top-btn');
    expect(title).toBeInTheDocument();
    expect(profile).toBeInTheDocument();
    expect(search).toBeInTheDocument();
  })
*/
