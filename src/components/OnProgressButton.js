import React, { useState } from 'react';

function OnProgressButton() {

function handleOnProgress() {
    const [statusDone, setStatusDone] = useState([])
    const [statusInProgress, setStatusInProgress] = useState([])
}

    return(
        <button
        onClick={}
        >

        </button>
    );
}